# Image Search Engine with Elasticsearch and Flask

This project aims to create an image search engine using Elasticsearch and Flask, with both frontend and backend components.

## Overview

- Set up an Elasticsearch server and Logstash to manage image data.
- Created an Elasticsearch index and populated it with data from the Flickr Photos dataset.

## Technologies Used

- Elasticsearch:version 8.1.0
- Flask 2.3.3
- Elasticsearch Python Client  Version 7.16.0

## Getting Started

To run the project locally, follow these steps:

1. Install Elasticsearch server and Logstash.
2. Create an Elasticsearch index and ingest data from the Flickr Photos dataset.
3. Set up a virtual environment (venv) for your Flask application.
4. Install project dependencies.
5. Run the Flask application.

## Usage

Once the project is set up and running, you can access the image search engine through the provided frontend interface. Users can search for images based on various criteria, making use of the Elasticsearch-powered backend for efficient searching.


